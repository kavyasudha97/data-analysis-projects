-- Question 1: Write a query of the `books` table that returns the top 100 results and includes `book_id`, `authors`, `title`, and `average_rating`. Use an alias for at least one column and sort the result set in descending order of rating. What is the number one book?
select * from dbo.books

select top 100 *, book_id, authors,title,average_rating as rating from dbo.books order by average_rating DESC

-- Question 2: Write a query to find the least popular book.

select book_id ,authors,title, average_rating from dbo.books order by average_rating asc

--Question 3: Which tag is the most popular?

select * from dbo.tags

SELECT  tag_name, COUNT(*) AS Tag_popular FROM dbo.tags GROUP BY tag_name ORDER BY Tag_popular DESC;

-- Question 4: What is the name of the most popular tag?

SELECT TOP 1 tag_name FROM dbo.tags GROUP BY tag_name ORDER BY COUNT(*) DESC;

-- Question 5: How many books where released in the first decade of 2000?

select sum(books_count) as first_decade2000 from dbo.books where original_publication_year>=2000 and original_publication_year<=2010

-- Question 6: How many book titles contain the word, "happy"?

select count(original_title) as happy_books from dbo.books where original_title like '%happy%'

-- Question 7: List the books from the top 3 authors from Question 1.  If there is more than one author just use the first one. 
-- Sort the title alphabetically by `author` and then by `average_rating`, best rated to lowest. Does this order matter in sorting?

select * from dbo.books 

SELECT authors, title, average_rating FROM dbo.books WHERE authors IN ( SELECT TOP 3 authors FROM dbo.books GROUP BY authors ORDER BY 
AVG(average_rating) DESC) ORDER BY authors ASC, average_rating DESC;

-- Question 8: Write a query that returns the number of authors whose first name is between rock and roll.

select distinct authors from dbo.books where authors between 'rock' and 'roll'

-- ## Part 2: Find the Answers to Your Own Questions

-- Your Question:  Click here to add a questions that you want to answer and write a query to find the answer!

-- question: write a query that returns which author has written more books
 
select authors, sum(books_count) from dbo.books group by authors order by sum(books_count) desc 

-- Your Question:  Click here to add a questions that you want to answer and write a query to find the answer!

-- question: write a query to retur whose original title is same as title

select distinct original_title, title from dbo.books where original_title = title















